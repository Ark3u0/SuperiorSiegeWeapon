using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameObjectCollectable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    public void OnTriggerEnter(Collider other)
    {/*
       // PlayerController controller = other.gameObject().GetComponent<>();
        if (controller)
        {
            // call an add Object Integer function in the player controller function 
            //Player Audio Feedback for pick up 
            //Dealte self drom the game 
           // delete(self); 
        }*/

    }
}
